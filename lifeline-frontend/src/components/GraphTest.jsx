// import React from "react";

// const GraphTest = ({ users }) => {
//   const simulation = d3.forceSimulation(nodes);
//   const nodes = data.map((user, index) => ({
//     name: user.name,
//     x: (width / (data.length + 1)) * (index + 1), // Evenly spaced nodes
//     y: height / 2, // Centered vertically
//   }));
//   return <div>GraphTest</div>;
// };

// export default GraphTest;
